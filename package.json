{
  "name": "pi-sentinel",
  "private": true,
  "version": "0.1.0",
  "description": "Safety guard for pi.dev — intercepts dangerous tool calls with configurable rules, user prompts, and audit logging",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
